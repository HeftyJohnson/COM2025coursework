<p id="notice"><%= notice %></p>

<h1 class="centerText">Entries</h1>

<table class="centerTable">
  <thead>
    <tr>
      <th>Team (ID)</th>
      <th>Race (ID)</th>
      <th colspan="3">Tools</th>
    </tr>
  </thead>

  <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= entry.team_id %></td>
        <td><%= entry.race_id %></td>
        <td><%= link_to 'Show', entry, :class => 'button' %></td>
        <td><%= link_to 'Edit', edit_entry_path(entry), :class => 'button' %></td>
        <td><%= link_to 'Destroy', entry, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'button' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div class="centerText">
  <%= link_to 'New Entry', new_entry_path, :class => 'button'%>
</div>

